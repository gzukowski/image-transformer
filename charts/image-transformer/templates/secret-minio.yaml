apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
type: Opaque
stringData:
  MINIO_ROOT_USER: {{ .Values.minio.rootUser | quote }}
  MINIO_ROOT_PASSWORD: {{ required "Set minio.rootPassword (--set minio.rootPassword=... or values-dev.yaml)" .Values.minio.rootPassword | quote }}
