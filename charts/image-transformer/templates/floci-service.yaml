apiVersion: v1
kind: Service
metadata:
  name: floci
  labels:
    app.kubernetes.io/name: floci
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: floci
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: edge
      port: 4566
      targetPort: 4566
